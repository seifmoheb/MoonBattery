<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Registered MoonBatteries</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f9;
      color: #333;
      margin: 0;
      padding: 20px;
    }
    h1 {
      text-align: center;
      color: #0066cc;
    }
    .moonbattery-list {
      max-width: 600px;
      margin: auto;
      padding: 10px;
      border: 1px solid #ccc;
      background-color: #ffffff;
      box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
    }
    .moonbattery-item {
      padding: 15px;
      border-bottom: 1px solid #ddd;
    }
    .moonbattery-item:last-child {
      border-bottom: none;
    }
    .moonbattery-item p {
      margin: 0;
      font-size: 14px;
    }
    .refresh-button {
      display: block;
      width: 100px;
      padding: 10px;
      margin: 20px auto;
      text-align: center;
      font-weight: bold;
      color: #ffffff;
      background-color: #0066cc;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s;
    }
    .refresh-button:hover {
      background-color: #005bb5;
    }
    .config-button {
   display: inline-block;
   padding: 8px 16px;
   margin-top: 10px;
   text-align: center;
   font-weight: bold;
   color: #ffffff;
   background-color: #0066cc;
   border-radius: 5px;
   text-decoration: none;
   transition: background-color 0.3s;
   cursor: pointer;
}
.config-button:hover {
   background-color: #005bb5;
}

  </style>
</head>
<body>
  <h1>Sonnen MoonBatteries</h1>
  <div class="moonbattery-list">
    <% Storage.all.each do |item| %>
      <div class="moonbattery-item">
        <p><strong>MacAddress:</strong> <%= item.macaddress %></p>
        <p><strong>SerialNumber:</strong> <%= item.serialnumber %></p>
        <p><strong>Last Contact:</strong> <%= item.lastcontact %></p>

        <a href="<%= config_item_path(macaddress: item.macaddress) %>" class="config-button">View Configurations</a>

      </div>
    <% end %>
  </div>
  <button class="refresh-button" onclick="location.reload();">Refresh</button>
</body>
</html>
