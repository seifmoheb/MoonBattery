<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Moonbattery Configurations</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f9;
      color: #333;
      margin: 0;
      padding: 20px;
    }
    h1 {
      text-align: center;
      color: #0066cc;
      font-size: 24px;
      margin-bottom: 20px;
    }
    .config-container {
      max-width: 600px;
      margin: auto;
      padding: 20px;
      background-color: #ffffff;
      border-radius: 8px;
      box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
    }
    .moonbattery-item {
      border: 1px solid #ddd;
      padding: 10px;
      margin-bottom: 10px;
      background-color: #f9f9f9;
      border-radius: 5px;
      font-size: 16px;
    }
    .config-button, .return-button {
      display: inline-block;
      padding: 10px 15px;
      color: white;
      font-size: 14px;
      font-weight: bold;
      text-decoration: none;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.3s;
    }
    .config-button {
      background-color: #4CAF50;
      margin-top: 10px;
    }
    .config-button:hover {
      background-color: #45a049;
    }
    .return-button {
      background-color: #0066cc;
      margin-top: 20px;
      text-align: center;
    }
    .return-button:hover {
      background-color: #005bb5;
    }
  </style>
</head>
<body>
  <div class="config-container">
    <h1>Moonbattery Configurations for <%= @passed_item_macaddress %></h1>

    <% @record.configurations.each do |config| %>
      <div class="moonbattery-item">
        <p><strong><%= config.configuration %>:</strong> <%= config.value %></p>
      </div>
    <% end %>

    <div style="text-align: center;">
      <a href="/" class="return-button">Return to Dashboard</a>
    </div>

  </div>
</body>
</html>
